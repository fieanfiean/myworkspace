# Set to staging in a deployed staging build. The badge is always shown in Vite dev mode.
VITE_APP_ENV=staging

# Copy this file to .env.local and replace each value with your Supabase project config.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
